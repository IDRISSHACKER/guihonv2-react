version: 2.1

jobs:
  init:
    docker:
      - image: python:latest
    steps:
      - checkout
      - run: python --version
      - run: pip --version
      - run: pip install ansible
      - run: ansible --version


workflows:
  ci-ci-guihon-front:
    jobs:
      - init
