version: 2.1

jobs:
  init:
    docker:
      - image: node:latest
    steps:
      - run: echo hello

  deploy:
    steps:
      - run: echo docker

workflows:
  ci-ci-guihon-front:
    jobs:
      - init
