version: 2.1

orbs:
  python: circleci/python@3.10.0

jobs:
  init:
    docker:
      - image: circleci/node
    steps:
      - run: echo hello

workflows:
  ci-ci-guihon-front:
    jobs:
      - init
