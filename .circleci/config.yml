version: 2.1

jobs:
  init:
    docker:
      - image: python:latest
    steps:
      - checkout
      - run: pip install ansible
      - run: ls


workflows:
  ci-ci-guihon-front:
    jobs:
      - init
